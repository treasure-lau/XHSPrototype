<?xml version="1.0" encoding="utf-8"?>
<FrameLayout android:id="@+id/recommend_user_layout_container"
    android:background="@color/background"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:textAppearance="@style/TextSmall"
        android:gravity="center_vertical"
        android:id="@+id/tv_layout_title"
        android:layout_width="wrap_content"
        android:layout_height="44.0dip"
        android:layout_marginLeft="15.0dip"
        android:drawableLeft="@drawable/ic_add_friends"
        android:drawablePadding="10.0dip" />

    <ImageView
        android:layout_gravity="right"
        android:id="@+id/btn_close"
        android:padding="4.0dip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10.0dip"
        android:layout_marginRight="15.0dip"
        android:src="@drawable/ic_recommend_close" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recommend_user_recyclerview"
        android:paddingLeft="8.0dip"
        android:paddingBottom="12.0dip"
        android:clipToPadding="false"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="44.0dip" />
</FrameLayout>